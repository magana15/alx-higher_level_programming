# Star Wars Movie Characters Script

This script retrieves and prints all characters from a Star Wars movie based on the provided movie ID. It utilizes the Star Wars API.

## Usage

1. Ensure you have Node.js installed on your system.
2. Install the required `request` module by running `npm install request`.
3. Run the script with the following command:


Replace `<movie_id>` with the ID of the Star Wars movie you want to retrieve characters for.

## Example

To retrieve characters from "Return of the Jedi" (movie ID: 3), run:


## Output

The script will print the names of all characters in the specified Star Wars movie, one character per line.

## Notes

- Ensure your system has an active internet connection to fetch data from the Star Wars API.
- If there are any errors during the process, such as failed API requests or parsing errors, the script will print error messages accordingly.

